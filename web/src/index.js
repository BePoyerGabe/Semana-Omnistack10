import React from 'react'; //em todo arquivo q for usar HTML ou JSX (js + xml)
import ReactDOM from 'react-dom'; //manipular a arvore de elementos da página (browser, muda se for mobile, tv)
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); // padrão, nunca vai mudar


